@model BookStore.Models.Domain.Publisher

<div class="container pt-5">
    <div class="row shadow rounded">
        <div class="col-md-6">
            <h1 class="fst-italic">Update Publisher</h1>
            <form asp-action="Update" method="post">
                <input type="hidden" asp-for="Id" />
                <div class="form-group">
                    <label asp-for="PublisherName">Publisher Name</label>
                    <input type="text" class="form-control" asp-for="PublisherName" />
                    <span asp-validation-for="PublisherName" class="text-danger"></span>
                </div>
                @if (TempData["msg"] != null)
                {
                    <div class="alert alert-info mt-3">
                        @TempData["msg"]
                    </div>
                }
                <div class="row mt-3">
                    <div class="col-sm-4 d-grid">
                        <button type="submit" class="btn btn-outline-primary"><i class="bi bi-save"></i>Update</button>
                    </div>
                    <div class="col-sm-4 d-grid">
                        <a href="/Publisher/GetAll" class="btn btn-outline-dark"><i class="bi-list"></i>List</a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


